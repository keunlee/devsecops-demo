apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: el-petclinic-config-webhook
  namespace: cicd
  labels:
    app.kubernetes.io/managed-by: EventListener
    app.kubernetes.io/part-of: Triggers
    eventlistener: webhook
spec:
  port:
    targetPort: http-listener
  to:
    kind: "Service"
    name: el-petclinic-config-webhook
    weight: 100
